1 + 2
"hi" <> "!"
List.map([1, 2, 3], fn(n) = n * n)
Optional.withDefault(Some(7), 0)
1 + "a"
1 / 0
Io.println("side effect")
let x = 21
x * 2
let f = fn(n) = n + x
let x = 100
f(0)
{ let y = 5; let y = y * 2; y }
it
let zero = fn() = 0
zero()
let plus = fn(n) = n + 1
List.map([1, 2], plus)
fn double(n) = n * 2
double(21)
fn double(n) = n * 3
double(21)
type Shape = Dot | Box(w : Int, h : Int)
fn area(s) = match s { Dot -> 0 | Box(w = w, h = h) -> w * h }
let box = Box(w = 2, h = 3)
area(box)
fn id(a) = a
#(id(1), id("a"))
abstract type Stack = S(List(Int)) let Stack.empty = S([]) fn Stack.push(s, n) = match s { S(ns) -> S(n :: ns) }
Stack.push(Stack.empty, 4)
S([])
fn f() = 1 let y = 2
type Pair = P(Int, Int)
:bindings
:t 1 + 2
:type Io.println
:set
:set timing off
:set depth 2
[[[1, 2], [3]], [[4]]]
:set depth 0
:set length 2
[1, 2, 3, 4]
:set length 0
:forget x
x
:forget nosuchname
:browse Optional
:doc String.trim
:zz
:b
:f
:t
:browse Nope
:load aaaa
:load Nope
:load List
:reload hhhh
:doc Nope.nope
:bindings
:forget *
:bindings
:help
:output /tmp/ern-output-test.log
Io.println("to the file")
:output
:output -
Io.println("to the tail")
:output nosuchdir/x.log
:quit
